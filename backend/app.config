# Configuration for Flask application

# Upload directory (modify if needed)
UPLOAD_FOLDER = '/tmp/upload'  # Adjust if you have a different upload directory

# Secret key for session management (replace with a random string)
SECRET_KEY = 'your_secret_key_here'

# Debug mode (set to False for production)
DEBUG = False

# WSGI server configuration (if using Gunicorn)
# Adjust these values based on your specific Gunicorn configuration
# and deployment environment
HOST = '0.0.0.0'  # Listen on all interfaces (optional)
PORT = 8080  # Default port for web applications

# Adjust these values if needed for Gunicorn worker processes
WORKERS = 1
THREADS_PER_WORKER = 1

# Configure environment variables (if needed)
# Replace with your actual environment variable names and values
# EXAMPLE_VAR = os.environ.get('EXAMPLE_VAR')
